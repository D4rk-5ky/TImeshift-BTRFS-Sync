"""timeshift-btrfs-sync package metadata.

This module is imported by the CLI to show the program version and to make the
folder a normal Python package.
"""

# Keep the version in one place so `ts-btrfs --version` and packaging agree.
__version__ = "0.1.1"
